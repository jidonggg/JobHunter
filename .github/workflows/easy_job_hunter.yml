name: 🧠 스마트 일자리 헌터

on:
  schedule:
    # 4시간마다 실행 (0시, 4시, 8시, 12시, 16시, 20시 KST 기준)
    - cron: '0 */4 * * *'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  smart-hunt:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 코드 가져오기
        uses: actions/checkout@v4
      
      - name: 💾 이전 데이터 복원
        uses: actions/cache@v4
        with:
          path: seen_jobs_v6.json
          key: seen-jobs-v6-${{ github.run_id }}
          restore-keys: seen-jobs-v6-
      
      - name: 🐍 Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 📦 패키지 설치
        run: pip install requests
      
      - name: 🧠 스마트 일자리 검색
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python easy_job_hunter.py
